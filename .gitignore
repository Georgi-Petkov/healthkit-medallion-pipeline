# Python
__pycache__/
*.pyc
.venv/
.python_packages/
.pytest_cache/
.ruff_cache/

# Azure Functions
local.settings.json
bin/
obj/

# dbt
target/
dbt_packages/
logs/
.user.yml

# Databricks Asset Bundles
.databricks/

# Terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfvars

# Jupyter
.ipynb_checkpoints/

# secrets - never commit these
.env
*.pem
*.key

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Playwright MCP's local trace cache (console logs, page snapshots) —
# pure local debugging output, regenerates automatically, never commit.
.playwright-mcp/
