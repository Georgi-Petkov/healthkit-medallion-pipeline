bundle:
  name: healthkit_pipeline

# This bundle is retired -- see PIPELINE_ARCHITECTURE.md's "Original design"
# section. Not deployed (Free Edition has no classic clusters, which this
# Lakeflow pipeline needs). Host below updated only so VS Code's Databricks
# extension, which auto-detects this folder as the workspace's only bundle,
# points at the real current workspace instead of the deleted Azure one.

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-8cc7279d-8e43.cloud.databricks.com
